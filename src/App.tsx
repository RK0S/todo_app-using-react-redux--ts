import { FC } from 'react';
import Wrapper from './components/list/Wrapper';

import './styles/app.css';

const App: FC = () => {
    return (
        <div className="app">
            <Wrapper />
        </div>
    );
};

export default App;
